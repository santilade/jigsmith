"""Jigsmith TUI — the workbench.

A starter-kit template, not a finished app. Clone, `rm -rf .git`, `git init`,
then grow it to fit how *you* drive coding agents. The base scaffold lives in
`core`/`base` dirs; everything you or the agent forge lands in `forged` dirs.
"""

__all__ = ["__version__"]
__version__ = "0.1.0"
