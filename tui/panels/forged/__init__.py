"""Your forged component *types* live here (rare).

Most extension is data, not code: the agent writes box specs into
config/profile.json using the existing components. You only add a module here
when you need a genuinely new *kind* of box. Each module defines a class
decorated with `@component("name")` from tui.panels.components.
"""
